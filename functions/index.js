const { initializeApp } = require("firebase-admin/app");

initializeApp();

// Callable functions are added here as the app's backend takes shape
// (generateContent, grantPurchase, deleteAccount, etc. — Phase 6/7).
